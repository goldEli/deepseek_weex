

```shell
# 运行获取账户资产脚本
uv run python3 get_account_assets.py;

# 运行获取订单历史脚本
uv run python3 fetch_order_history.py;

# 运行获取当前持仓脚本
uv run python3 get_current_positions.py

# 运行查询订单历史脚本
uv run python3 query_history_orders.py;
```